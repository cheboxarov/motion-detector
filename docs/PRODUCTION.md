# Инструкция по развертыванию и тестированию (Production)

## 🚀 Быстрый старт

### 1. Подготовка

Создайте файл `.env.prod` на основе примера:

```bash
cp .env.prod.example .env.prod
```

Отредактируйте `.env.prod` и установите безопасные пароли:

```bash
nano .env.prod  # или используйте любой редактор
```

### 2. Запуск приложения

```bash
# Используйте скрипт запуска
./scripts/start.sh

# Или вручную
docker-compose -f docker/docker-compose.prod.yml --env-file .env.prod up -d --build
```

### 3. Проверка статуса

```bash
docker-compose -f docker/docker-compose.prod.yml ps
```

Все сервисы должны быть в состоянии `Up` и `healthy`.

---

## 🌐 Доступ к сервисам

После запуска следующие сервисы будут доступны:

| Сервис | URL | Описание |
|--------|-----|----------|
| **FastAPI App** | http://localhost:8000 | Основное приложение API |
| **API Docs (Swagger)** | http://localhost:8000/docs | Интерактивная документация API |
| **API Docs (ReDoc)** | http://localhost:8000/redoc | Альтернативная документация API |
| **Prometheus** | http://localhost:9090 | Метрики и мониторинг |
| **Grafana** | http://localhost:3000 | Визуализация метрик |

**Учетные данные Grafana:**
- Логин: `admin` (или значение из `GRAFANA_ADMIN_USER`)
- Пароль: значение из `GRAFANA_ADMIN_PASSWORD` в `.env.prod`

---

## 🧪 Тестирование API

### Вариант 1: Использование Postman

1. **Импорт коллекции:**
   - Откройте Postman
   - Нажмите `Import`
   - Выберите файл `docs/api/Postman_Collection_Video_Analysis.json`
   - Коллекция будет импортирована с переменными:
     - `base_url`: http://localhost:8000
     - `video_id`: автоматически сохраняется после загрузки видео

2. **Тестирование:**
   - **Health Check**: Проверка здоровья приложения
   - **Get Metrics**: Просмотр Prometheus метрик
   - **Upload Video for Analysis**: Загрузите видео файл (mp4, avi, etc.)
   - **Get Analysis Result**: Получите результат анализа (использует сохраненный `video_id`)

### Вариант 2: Использование curl

#### 1. Health Check
```bash
curl http://localhost:8000/health
```

**Ожидаемый ответ:**
```json
{
  "status": "healthy",
  "database": "connected"
}
```

#### 2. Получение метрик Prometheus
```bash
curl http://localhost:8000/metrics
```

**Ожидаемый ответ:** Метрики в формате Prometheus text format

#### 3. Загрузка видео для анализа
```bash
curl -X POST http://localhost:8000/analyze \
  -F "file=@/path/to/your/video.mp4"
```

**Ожидаемый ответ:**
```json
{
  "video_id": "123e4567-e89b-12d3-a456-426614174000",
  "status": "pending",
  "message": "Видео принято в обработку"
}
```

**Важно:** Сохраните `video_id` для следующего запроса!

#### 4. Получение результата анализа
```bash
# Замените VIDEO_ID на реальный ID из предыдущего ответа
curl http://localhost:8000/results/VIDEO_ID
```

**Ожидаемый ответ (pending):**
```json
{
  "id": "123e4567-e89b-12d3-a456-426614174000",
  "filename": "video.mp4",
  "upload_time": "2024-01-01T12:00:00",
  "analysis_time": null,
  "has_motion": null,
  "processing_duration_ms": null,
  "status": "pending",
  "error_message": null
}
```

**Ожидаемый ответ (completed):**
```json
{
  "id": "123e4567-e89b-12d3-a456-426614174000",
  "filename": "video.mp4",
  "upload_time": "2024-01-01T12:00:00",
  "analysis_time": "2024-01-01T12:00:05",
  "has_motion": true,
  "processing_duration_ms": 3500,
  "status": "completed",
  "error_message": null
}
```

### Вариант 3: Использование Swagger UI

1. Откройте http://localhost:8000/docs
2. Все endpoints доступны для интерактивного тестирования
3. Нажмите на endpoint → `Try it out` → Заполните параметры → `Execute`

---

## 📊 Мониторинг

### Prometheus

1. Откройте http://localhost:9090
2. В поиске введите метрики:
   - `video_processed_total` - общее количество обработанных видео
   - `video_processing_duration_seconds` - время обработки видео
   - `video_errors_total` - количество ошибок
   - `videos_in_queue` - количество видео в очереди

### Grafana

1. Откройте http://localhost:3000
2. Войдите с учетными данными из `.env.prod`
3. Prometheus datasource уже настроен автоматически
4. Создайте дашборд или используйте готовые панели

**Пример запроса для Grafana:**
```promql
rate(video_processed_total[5m])
```

---

## 🔍 Проверка логов

### Просмотр логов всех сервисов
```bash
docker-compose -f docker/docker-compose.prod.yml logs -f
```

### Просмотр логов конкретного сервиса
```bash
# Логи приложения
docker-compose -f docker/docker-compose.prod.yml logs -f app

# Логи PostgreSQL
docker-compose -f docker/docker-compose.prod.yml logs -f postgres

# Логи Prometheus
docker-compose -f docker/docker-compose.prod.yml logs -f prometheus

# Логи Grafana
docker-compose -f docker/docker-compose.prod.yml logs -f grafana
```

---

## 🛑 Остановка

### Остановка без удаления данных
```bash
docker-compose -f docker/docker-compose.prod.yml down
```

### Остановка с удалением данных (⚠️ ОСТОРОЖНО!)
```bash
docker-compose -f docker/docker-compose.prod.yml down -v
```

---

## 📝 Статусы обработки видео

- **pending**: Видео принято, ожидает обработки
- **processing**: Видео обрабатывается
- **completed**: Обработка завершена успешно
- **failed**: Ошибка при обработке

---

## 🔧 Troubleshooting

### Проблема: Сервис не запускается

**Решение:**
```bash
# Проверьте логи
docker-compose -f docker/docker-compose.prod.yml logs [service_name]

# Пересоберите образы
docker-compose -f docker/docker-compose.prod.yml build --no-cache

# Перезапустите
docker-compose -f docker/docker-compose.prod.yml up -d
```

### Проблема: База данных не подключается

**Решение:**
```bash
# Проверьте, что PostgreSQL запущен и healthy
docker-compose -f docker/docker-compose.prod.yml ps postgres

# Проверьте переменные окружения
docker-compose -f docker/docker-compose.prod.yml config | grep DATABASE_URL
```

### Проблема: Миграции не применяются

**Решение:**
```bash
# Запустите миграции вручную
docker-compose -f docker/docker-compose.prod.yml exec app alembic -c app/alembic.ini upgrade head
```

---

## 📦 Форматы поддерживаемых видео

- MP4 (рекомендуется)
- AVI
- MOV
- MKV
- И другие форматы, поддерживаемые OpenCV

---

## 🎯 Полный цикл тестирования

1. **Проверьте health:**
   ```bash
   curl http://localhost:8000/health
   ```

2. **Загрузите видео:**
   ```bash
   curl -X POST http://localhost:8000/analyze -F "file=@test_video.mp4"
   ```

3. **Подождите 5-10 секунд** (обработка в фоне)

4. **Проверьте результат:**
   ```bash
   curl http://localhost:8000/results/VIDEO_ID
   ```

5. **Проверьте метрики:**
   ```bash
   curl http://localhost:8000/metrics | grep video_processed_total
   ```

---

## 📚 Дополнительная информация

- **API Документация**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc
- **Prometheus**: http://localhost:9090
- **Grafana**: http://localhost:3000

---

## ✅ Чек-лист готовности к продакшену

- [ ] Изменены пароли в `.env.prod` на безопасные
- [ ] Проверены все endpoints
- [ ] Проверены метрики в Prometheus
- [ ] Настроен Grafana dashboard
- [ ] Проверены логи на наличие ошибок
- [ ] Настроены бэкапы для PostgreSQL
- [ ] Настроен мониторинг и алерты (опционально)

---

**Удачного тестирования! 🚀**

